/**
 * @typedef {Object} CalculateInputDTO
 * @property {number} a
 * @property {number} b
 * @property {'add' | 'subtract'} operation
 */

/**
 * @typedef {Object} CalculateOutputDTO
 * @property {number} result
 */

export {}; // types-only module
